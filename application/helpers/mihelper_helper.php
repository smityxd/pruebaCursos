<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');
/*
if ( ! function_exists(''))
{
    function maxId($tabla = '')
    {
        $ci =& get_instance();
        $sql = "select max(id) as idx from ".$tabla;
        $query = $ci->db->query($sql);
        $row = $query->row();
        if(isset($row)){
            return $row->idx;
        }
    }
}
*/